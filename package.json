{
  "name": "fmdb",
  "version": "1.0.0",
  "description": "Fastify MariaDB plugin",
  "main": "index.js",
  "author": "Marin Bivol <marin.bivol@fourtheorem.com>",
  "scripts": {
    "mariadb": "docker run -d -p 3306:3306 -e MYSQL_ALLOW_EMPTY_PASSWORD=yes --rm mariadb",
    "test": "tap *.test.js --coverage --reporter=spec"
  },
  "dependencies": {
    "fastify-plugin": "^4.3.0",
    "mariadb": "^3.0.2",
    "sqlstring": "^2.3.3"
  },
  "devDependencies": {
    "fastify": "^4.10.0",
    "tap": "^16.3.2"
  },
  "publishConfig": {
    "registry":"https://npm.pkg.github.com/",
    "access": "public"
  }
}
